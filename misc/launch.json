{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug main.cpp",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/main",
            "args": [],
            "cwd": "${fileDirname}",
            "preLaunchTask": "build for debug"
        }
    ]
}
